experiment_name: "sst2_distilbert"
output_dir: "results/main/benchmark"

data:
  dataset_name: "sst2"
  train_samples: -1  # full split: 67,349
  test_samples: -1   # full split: 872

model:
  name: "answerdotai/ModernBERT-base"

evaluation:
  seeds: [42]

mechanistic:
  circuit_fraction: 0.1
