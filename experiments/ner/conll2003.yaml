experiment_name: "ner_conll2003"
output_dir: "results/ner_conll2003"

data:
  dataset_name: "conll2003"
  train_samples: -1
  test_samples: -1

model:
  name: "answerdotai/ModernBERT-base"

evaluation:
  seeds: [42]

training:
  sparsity_target: 0.01
  warmup_fraction: 0.3
  batch_size: 64
