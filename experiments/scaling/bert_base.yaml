experiment_name: "sst2_bert_base"
output_dir: "results/scaling/bert_base"

data:
  dataset_name: "sst2"
  train_samples: 2000
  test_samples: 200

model:
  name: "bert-base-uncased"

evaluation:
  seeds: [42]
  explainers: ["splade", "lime", "ig", "gradient_shap", "attention", "saliency", "deeplift", "random"]
