experiment_name: "sst2_bert_large"
output_dir: "results/scaling/bert_large"

data:
  dataset_name: "sst2"
  train_samples: 2000
  test_samples: 200

model:
  name: "bert-large-uncased"

evaluation:
  seeds: [42]
  explainers: ["splade", "lime", "ig", "gradient_shap", "attention", "saliency", "deeplift", "random"]
