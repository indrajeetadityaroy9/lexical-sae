experiment_name: "imdb_distilbert"
output_dir: "results/datasets/imdb"

data:
  dataset_name: "imdb"
  train_samples: -1  # full split: 25,000
  test_samples: -1   # full split: 25,000

model:
  name: "answerdotai/ModernBERT-base"

evaluation:
  seeds: [42]
