experiment_name: "surgery"
output_dir: "results/surgery"

data:
  dataset_name: "civilcomments"
  train_samples: 10000
  test_samples: 2000

model:
  name: "answerdotai/ModernBERT-base"

evaluation:
  seeds: [42]

mechanistic:
  circuit_fraction: 0.1
